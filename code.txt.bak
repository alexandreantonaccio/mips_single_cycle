# Inicializa $t0 = 3
20100003   # addi $t0, $zero, 3

# Inicializa $t1 = 5
20110005   # addi $t1, $zero, 5

# slti $t2, $t0, 5 -> $t2 = ($t0 < 5) ? 1 : 0 => 1
29020005   # slti $t2, $t0, 5

# slti $t3, $t1, 5 -> $t3 = ($t1 < 5) ? 1 : 0 => 0
29030005   # slti $t3, $t1, 5

# slti $t4, $t1, 10 -> $t4 = ($t1 < 10) ? 1 : 0 => 1
2904000A   # slti $t4, $t1, 10

# Fim do programa: jump para si mesmo
08000008   # j 0x00000008
